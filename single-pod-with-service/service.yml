apiVersion: v1
kind: Service
metadata:
  name: my-first-static-website-service
spec:
  type: NodePort
  selector:
    app: my-first-static-website
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30010
